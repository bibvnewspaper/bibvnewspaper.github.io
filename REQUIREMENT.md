# Requirements
Home Page:
- Just structure like normal Markdown text (with header 1's header 2's, normal text)
- Add some code and some latex equations

Navigation Bar
- Just add a plain navigation bar at the top with "Home" button